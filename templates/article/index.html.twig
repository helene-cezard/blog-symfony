{% extends 'base.html.twig' %}

{% block body %}

<div class="article">
    <h1>{{ article.title }}</h1>
    <p>écrit par {{ article.author.username }}</p>
    <p>{{ article.content }}</p>
</div>
{% endblock %}
