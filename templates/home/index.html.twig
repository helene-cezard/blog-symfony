{% extends 'base.html.twig' %}

{% block body %}

    {% for article in articles %}
        <div class="card mx-auto my-4">
        <img src="{{ article.image }}" class="card-img-top" alt="...">
        <div class="card-body">
            <h2 class="card-title text-center">{{ article.title }}</h2>
            <p> par {{ article.author.pseudonyme }}</p>
            <p class="card-text">{{ article.content }}</p>
            <a href="{{ path ( 'article_show', {id:article.id} ) }}" class="btn btn-primary">Continuer à lire</a>

            {% if ( app.user == article.author) or is_granted('ROLE_ADMIN') %}

            <a href="{{ path( 'article_update', {id:article.id} ) }}" class="btn btn-primary">Modifier cet article</a>
            <a href="{{ path( 'article_delete', {id:article.id} ) }}" class="btn btn-primary">Supprimer cet article</a>
            
            {% endif %}
        </div>
        </div>
    {% endfor %}

{% endblock %}
